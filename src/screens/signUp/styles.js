import { StyleSheet } from 'react-native';

/**
 * Export stylesheet
 */
export default Styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    paddingTop: 50,
  },
  title: {
    fontSize: 18,
    fontWeight: 'bold',
  },
  backButton: {
    marginTop: 30,
    alignItems: 'center'
  }
});
